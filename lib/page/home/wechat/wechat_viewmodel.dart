import 'package:flutter_easyrefresh/easy_refresh.dart';
import 'package:flutter_wananzhuo/base/base_change_notifier.dart';
import 'package:flutter_wananzhuo/view/load_layout.dart';
import 'wechat_repository.dart';
import 'package:flutter_wananzhuo/model/wx_offical_entity.dart';
import 'package:flutter/material.dart';
class WxChatViewModle extends BaseChangeNotifier{
  // LoadStatus loadState = LoadStatus.loading;
  // final repository = Wechatepository();
  // List<WxOfficalData> wxOfficalData =  [];
  // TabController? tabController
  // void getWxOff() {
  //   repository.getWxtOfficial().then((wxArticle) {
  //     if (wxArticle != null) {
  //         loadState = LoadStatus.content;
  //         wxOfficalData = wxArticle;
  //
  //         _tabs.addAll(_articList
  //             .map((e) => Tab(
  //           text: e.name,
  //         ))
  //             .toList());
  //         tabController =  TabController(vsync: this, length: _tabs.length);
  //     }
  //   }).catchError((e, s) {
  //       loadState = LoadStatus.netError;
  //   }).whenComplete(() {
  //     notifyListeners();
  //   });
  // }
  
  
  

  
}